---
backup_dir_remote: /var/tmp/aap_migration_backups
backup_label: ""

# Current installer directory (must contain setup.sh)
aap_installer_dir: ""
aap_installer_inventory: "inventory"

# Optional backup command path. If empty, defaults to <aap_installer_dir>/setup.sh
aap_backup_command: ""

# Local artifacts directory (control node)
artifact_dir_local: "{{ playbook_dir }}/../reports"
